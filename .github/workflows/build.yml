# name: Deploy to GHCR

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch: {}  # optional: allow manual runs

# jobs:
#   ghcr:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#       id-token: write   # keeps provenance working
#     env:
#       REGISTRY: ghcr.io
#       IMAGE_NAME: ${{ github.repository }}          # owner/repo (we'll lowercase later)
#       PLATFORMS: linux/amd64                        # <- set your target platforms here

#     steps:
#       - name: Check out codebase
#         uses: actions/checkout@v5

#       - name: Set environment variables
#         run: |
#           echo "BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_ENV
#           echo "BASEOS_NAME=$(jq -r .baseos.name baseos_digest)" >> $GITHUB_ENV
#           if [ -z "$(jq -r .baseos.digest baseos_digest)" ]; then
#             echo "BASEOS_DIGEST=" >> $GITHUB_ENV
#           else
#             echo "BASEOS_DIGEST=@$(jq -r .baseos.digest baseos_digest)" >> $GITHUB_ENV
#           fi
#           # Lowercased image path for GHCR
#           echo "IMAGE_URI=${{ env.REGISTRY }}/$(echo '${{ env.IMAGE_NAME }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Create Docker metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.IMAGE_URI }}
#           tags: |
#             type=raw,value=latest,enable={{is_default_branch}}
#             type=sha,format=long
#           labels: |
#             org.opencontainers.image.created=${{ env.BUILD_DATE }}
#             org.opencontainers.image.source=${{ github.event.repository.html_url }}
#             org.opencontainers.image.title=${{ github.repository }}
#             org.opencontainers.image.base.digest=${{ env.BASEOS_DIGEST }}
#             org.opencontainers.image.base.name=${{ env.BASEOS_NAME }}

#       - name: Build and push image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           build-args: |
#             BASEOS_DIGEST=${{ env.BASEOS_DIGEST }}
#           labels: ${{ steps.meta.outputs.labels }}
#           platforms: ${{ env.PLATFORMS }}
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           provenance: mode=max
#           sbom: true